syntax = "proto3";

package com.jalebi.proto.generated;

service Heartbeat {
    rpc ThumpThump (HeartbeatTrigger) returns (stream Beat) {}
}

message HeartbeatTrigger {
    int64 start = 1;
    int64 interval = 2;
}

message Beat {
    int64 epoch = 1;
}
